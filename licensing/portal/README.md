# Styrhous licensing portal

Static, client-only SvelteKit portal for Styrhous account and license management.

```bash
npm ci
npm test
```

Run the commands from the repository Nix development shell so Playwright uses the pinned browser
bundle.
