TODO list:

* test reduced permission handling
* Resource details view
* Add explicit support for Helm
* Display resource usages of pods
* Connect to kubernetes cluster by discovering via the Az cli/AWS cli/Tailscale
* Search
    * Add option for whole word matching only in search
    * Add option for case-sensitive matching in search
* For "CronJobs": Add option to start a job run right now
* Table improvements
    * Make columns on table user configurable (And remember per resource type in user's config folder)
    * Allow resizing the columns in a table on-the-fly
* Make the accessibility check require a "label" on all interactable elements, such as input fields or buttons.
* Double-check for "busy waits" in tests
* Allow extracting annotations/labels to be displayed in custom columns in the table view
* BUG: If you open the resource inspector, and then right click to customize the columns, that open a new bladestack, rather than extending the existing one. We need to ensure the application can only ever have 1 global blade stack. 

Deferred
* Make the "TailwindTable" handle sizes more automatically so we don't have to sit and pixel fiddle.